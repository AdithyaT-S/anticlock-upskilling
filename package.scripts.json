{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",

    "db:migrate":       "tsx scripts/migrate.ts",
    "db:migrate:reset": "tsx scripts/migrate.ts --reset",
    "db:migrate:dry":   "tsx scripts/migrate.ts --dry-run",
    "db:status":        "tsx scripts/migrate.ts --status",
    "db:seed":          "tsx scripts/seed.ts",
    "db:studio":        "open http://localhost:8080",

    "docker:up":    "docker compose up -d",
    "docker:down":  "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d",
    "docker:logs":  "docker compose logs -f nextjs",

    "test":     "vitest run",
    "test:e2e": "playwright test",
    "typecheck": "tsc --noEmit"
  },
  "comment": "Add your actual dependencies — this is the scripts block only"
}
